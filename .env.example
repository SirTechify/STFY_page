# Copy this file to .env and fill in SMTP credentials for the
# contact-form and subscribe-list mail backends. The app boots fine
# without these; mail-sending endpoints fail quietly.
MAIL_USERNAME=
MAIL_PASSWORD=
